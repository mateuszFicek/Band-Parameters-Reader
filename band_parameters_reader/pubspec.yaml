name: band_parameters_reader
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flutter_bloc: ^6.0.5
  flutter_screenutil: ^2.3.1
  google_fonts: ^1.1.0
  path_provider: ^1.6.14
  flutter_blue: ^0.7.2
  toast: ^0.1.5
  intl: ^0.16.1
  encrypt: ^4.1.0
  flutter_custom_clippers: ^1.1.2
  charts_flutter: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
